[all:vars]
ansible_connection=local

# OpenShift cluster configuration
openshift_cluster_name=openshift-cluster
openshift_base_domain=example.com
openshift_cluster_network_cidr=10.128.0.0/14
openshift_service_network_cidr=172.30.0.0/16
openshift_host_network_cidr=10.0.0.0/16

# Compact cluster configuration (0 compute nodes)
openshift_compute_replicas=0

# Local installation host
[localhost]
localhost ansible_connection=local

# Bootstrap node (for reference - not used in this playbook)
[bootstrap]
# bootstrap.example.com

# Control plane nodes (for reference - not used in this playbook)
[masters]
# master-0.example.com
# master-1.example.com
# master-2.example.com

# Compute nodes (empty for compact cluster - for reference)
[workers]
# worker-0.example.com
# worker-1.example.com

# Load balancer (optional - for reference)
[loadbalancers]
# lb.example.com

# OpenShift nodes (all nodes that will be part of the cluster - for reference)
[openshift_nodes:children]
masters
workers

# OpenShift cluster nodes (for reference)
[openshift_cluster:children]
masters
workers